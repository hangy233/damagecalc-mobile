import {ABILITIES} from '../lang';

const ABILITIES_ARRAY = Object.keys(ABILITIES);

export const ABILITIES_ADV = ABILITIES_ARRAY.slice(0, 71);
export const ABILITIES_DPP = ABILITIES_ARRAY.slice(0, 116);
export const ABILITIES_BW = ABILITIES_ARRAY.slice(0, 155);
export const ABILITIES_XY = ABILITIES_ARRAY.slice(0, 181);
export const ABILITIES_SM = ABILITIES_ARRAY.slice(0, 224);
